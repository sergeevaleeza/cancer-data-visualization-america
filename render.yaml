services:
  - type: web
    name: cancer-viz
    env: python
    plan: free
    branch: main
    pythonVersion: 3.11.5
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    regions:
      - oregon
